# --- CONFIGURACIÓN DEL BACKEND (TeamBoard) ---

# URL de la base de datos (SQLite por defecto, o puedes usar Postgresql/MySQL)
DATABASE_URL=sqlite:///./teamboard.db

# API Key de Mandrill/Mailchimp para envío de correos
MANDRILL_API_KEY=md-3qzWPga9BIEonCBs0Vt3rw'

# Remitente de los correos
FROM_EMAIL=noreply@verisure.cl
FROM_NAME=Bitácora · Customer Intelligence

# URL pública o local de la aplicación Frontend
APP_URL=http://localhost:5173

# Modo de pruebas para correos (True: envía todos los mails a TEST_EMAIL | False: envía a los destinatarios reales)
TEST_MODE=True
TEST_EMAIL=pablo.magana@verisure.cl
